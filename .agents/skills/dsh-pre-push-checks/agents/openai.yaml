interface:
  display_name: "DSH Pre-Push Checks"
  short_description: "Run focused and primary CI checks before push"
  default_prompt: "Use $dsh-pre-push-checks before pushing this DeepSeek Harness branch."
